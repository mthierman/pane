export class Event {
    // static new<T extends Record<string, any>>(type: keyof T, detail: T[keyof T]) {
    //     return new CustomEvent(type as string, { detail });
    // }
    // static new<T>(event: WebViewMessageEvent<WebViewMessageEventData<T>>) {
    //     return new CustomEvent(event.data.type as string, { detail: event.data.payload });
    // }
}
