{
    "name": "pane",
    "private": true,
    "type": "module",
    "scripts": {
        "check": "tsc -b",
        "example": "vite examples/webview/gui",
        "example:host": "vite examples/webview/gui --host",
        "example:build": "tsc -b && vite build examples/webview/gui",
        "example:watch": "vite build examples/webview/gui --watch",
        "example:preview": "tsc -b && vite build examples/webview/gui && vite preview examples/webview/gui"
    },
    "devDependencies": {
        "@types/node": "^24.2.1",
        "prettier": "^3.6.2",
        "typescript": "^5.9.2",
        "vite": "^7.1.2"
    }
}
